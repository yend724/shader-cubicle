import { LearningLayout } from '@/components/layout/LearningLayout';
import { ShaderCodeMirrorWithCanvas } from "@/components/ui/ShaderCodeMirrorWithCanvas";

export const meta = {
  title: "単色で塗りつぶす",
  order: 1,
  published: "2022-10-26",
  author: "YEND",
  tag: ["fragmentShader"]
}
export default ({ children }) => (
  <LearningLayout meta={meta}>{children}</LearningLayout>
);

まずは単色で塗りつぶしてみる。

色を変更するために`fragmentShader`を書き換える。

```glsl
void main () {
  // 黒色が表示される
  gl_FragColor = vec4(0.0, 0.0, 0.0, 1.0);
}
```

`gl_FragColor`には4次元ベクトルのvec4型が格納されている。四つの引数はそれぞれ、Red（赤）、Green（緑）、Blue（青）、Alpha（透過度）に対応している。

```glsl
void main () {
  // vec4(Red, Green, Blue, Alpha)
  // これは水色になる
  gl_FragColor = vec4(0.0, 1.0, 1.0, 1.0);
}
```

## 実行結果

実際に`fragmentShader`を書き換えてみると、色が変更されるのがわかる。

<ShaderCodeMirrorWithCanvas
  vertexShader={`void main () {
  gl_Position = projectionMatrix * viewMatrix * modelMatrix * vec4(position, 1.0);
}`}
  vertexHidden={true}
  fragmentShader={`void main () {
  gl_FragColor = vec4(0.0, 1.0, 1.0, 1.0);
}`}
/>